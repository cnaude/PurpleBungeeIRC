apply plugin: 'java'
apply plugin: 'maven'

group = 'com.cnaude'
def jenkinsBuild = System.getenv("BUILD_NUMBER") ?: "SNAPSHOT"
version = "0.1.${jenkinsBuild}"

description = 'PurpleBungeeIRC'

sourceCompatibility = 1.8
targetCompatibility = 1.8

repositories {
        
     maven { url "http://cnaude.org/repo/" }
     maven { url "https://oss.sonatype.org/content/repositories/snapshots" }
     maven { url "http://nexus.codecrafter47.dyndns.eu/content/repositories/public/" }
     maven { url "https://maven.jnc.world" }
     maven { url "https://repo.maven.apache.org/maven2" }
}
dependencies {
    compile group: 'com.cnaude.pircbotx', name: 'pircbotx-shaded', version:'2.1.8'
    compile group: 'com.cnaude.herochat', name: 'HerochatBungeeBridge', version:'1.1.0'
    compile group: 'commons-io', name: 'commons-io', version:'2.4'
    compile(group: 'net.md-5', name: 'bungeecord-api', version:'1252') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'net.md-5', name: 'bungeecord', version:'1252') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'codecrafter47.bungeetablistplus', name: 'bungeetablistplus-api-bungee', version:'2.8.1') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'dev.aura.bungeechat', name: 'BungeeChat', version:'2.1.6.855') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
}
